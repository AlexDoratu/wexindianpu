.name { font-size: 32rpx; color: var(--text-primary); font-weight: 600; }
.intro { font-size: 24rpx; color: var(--text-secondary); margin-top: 12rpx; }
.link { margin-top: 16rpx; color: var(--accent); font-size: 24rpx; }
